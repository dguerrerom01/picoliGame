package test;

import java.awt.Color;
import java.util.Collection;
import java.util.Iterator;

import modelo.Colores;
import vista.JLabelModel;

public class AccionListaUITest {
	
	public AccionListaUITest(ListaUITest listaUITest) {
		super();
//		Colores color=(Colores) listaUITest.getComboBox().getSelectedItem();
//		listaUITest.getGame().addMonedaEnLista(color);
//		Collection<Colores> paraLista=listaUITest.getGame().dameValoresLista();
//		listaUITest.getListaUI().removeAll();
//		for (Iterator iterator = paraLista.iterator(); iterator.hasNext();) {
//			Colores colores = (Colores) iterator.next();
//			listaUITest.getListaUI().add(new JLabelModel(colores.getColor()));
//		}
//		listaUITest.revalidate(); 
	}

}
